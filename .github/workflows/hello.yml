name: say hello
on:
  push:
    branches:
      - main
jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "Hello, Jonathan!"
      - name: Dime la fecha
        run: ls -l